{
  "name": "hie-calculator-android",
  "private": true,
  "version": "2.1.0",
  "description": "Native Android shell (Capacitor) for the HIE Calculator web app",
  "scripts": {
    "build": "node -e \"require('fs').accessSync('www/index.html'); console.log('web assets are static (remote-URL wrapper)')\""
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.2.0"
  }
}
